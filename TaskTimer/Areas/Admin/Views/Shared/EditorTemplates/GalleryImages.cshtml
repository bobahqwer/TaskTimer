@model TaskTimer.Models.GalleryImages

                <tr>
                    <td>
                        @Html.HiddenFor(m => Model.Id)
                        <div class="galleryImageText">Image name: @Model.Name</div>
                    </td>
                    <td></td>
                </tr>
                <tr>
                    <td>@Html.LabelFor(m => Model.Alt)</td>
                    <td>@Html.TextBoxFor(m => Model.Alt)</td>
                </tr>
                <tr>
                    <td>@Html.LabelFor(m => Model.BottomText)</td>
                    <td>@Html.TextBoxFor(m => Model.BottomText)</td>
                </tr>
        